export interface IActionSelector {
  action_type_id: number;
  name: string;
}

export interface IParameterSelector {
  parameter_type_id: number;
  name: string;
}

export interface IApplicationSelector {
  application_id: number;
  application_name: string;
}
